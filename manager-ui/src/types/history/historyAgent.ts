export interface HistoryAgent {
  id: string;
  createdAt: string;
  // unit?: string;
  // unitCode?: string;
  // zayadId?: number;
  // platformId?: number;
  // platform?: string;
  // firstSeenAt?: string;
  // lastSeenAt?: string;
}
